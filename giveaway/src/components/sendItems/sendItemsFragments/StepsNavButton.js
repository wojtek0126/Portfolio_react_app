import React from 'react';

const StepsNavButton = ({text}) => {
    return (
        <button className="btn steps-button yellow-hover-btn" type="submit">{text}</button>
    )
}

export default StepsNavButton;